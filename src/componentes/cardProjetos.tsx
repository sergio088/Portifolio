interface props {
  img?: React.ReactNode;
  t: string;
  git: string;
  tec: string[];
  live: string;
}

export default function Cardprojetos({ img, t, git, tec, live }: props) {
  return (
    <div>
      <h1>porra</h1>
    </div>
  );
}
